name: Publish to PyPI

on:
  push:
    tags:
      # Match tags like: omniscient-core-v0.2.0, omniscient-llm-v0.2.0rc1
      - 'omniscient-*-v*'
      # Match meta-package tags: v0.2.0
      - 'v*'
  # Allow manual trigger for testing or emergency fixes
  workflow_dispatch:
    inputs:
      package:
        description: 'Package to publish (core, llm, agents, tools, github, api, rag, or meta)'
        required: true
        default: 'core'
        type: choice
        options:
          - core
          - llm
          - agents
          - tools
          - github
          - api
          - rag
          - meta
      version:
        description: 'Version string (without v, e.g., 0.2.0). Required for manual runs.'
        required: true
        default: '0.2.0'
      dry_run:
        description: 'Dry run (build only, no publish)'
        type: boolean
        default: true

env:
  PYTHON_VERSION: "3.11"

jobs:
  # ============================================================================
  # Determine which package to publish based on tag or input
  # ============================================================================
  prepare:
    name: Prepare Release
    runs-on: ubuntu-latest
    outputs:
      package: ${{ steps.parse.outputs.package }}
      version: ${{ steps.parse.outputs.version }}
      package_path: ${{ steps.parse.outputs.package_path }}
      dry_run: ${{ steps.parse.outputs.dry_run }}
      is_manual: ${{ steps.parse.outputs.is_manual }}
    steps:
      - name: Parse tag or input
        id: parse
        run: |
          # --- Manual Trigger Logic ---
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            PKG="${{ github.event.inputs.package }}"
            VER="${{ github.event.inputs.version }}"
            DRY_RUN="${{ github.event.inputs.dry_run }}"
            
            echo "is_manual=true" >> $GITHUB_OUTPUT
            echo "dry_run=$DRY_RUN" >> $GITHUB_OUTPUT
            echo "version=$VER" >> $GITHUB_OUTPUT

            if [ "$PKG" = "meta" ]; then
              echo "package=omniscient-architect" >> $GITHUB_OUTPUT
              echo "package_path=." >> $GITHUB_OUTPUT
            else
              echo "package=omniscient-$PKG" >> $GITHUB_OUTPUT
              echo "package_path=packages/$PKG" >> $GITHUB_OUTPUT
            fi

          # --- Tag Trigger Logic ---
          else
            TAG="${GITHUB_REF#refs/tags/}"
            echo "Tag: $TAG"
            echo "is_manual=false" >> $GITHUB_OUTPUT
            echo "dry_run=false" >> $GITHUB_OUTPUT
            
            # Regex Explanation:
            # ^v(.*) matches v0.2.0, v0.2.0rc1
            # ^omniscient-([a-z]+)-v(.*) matches omniscient-core-v0.2.0-beta
            
            if [[ "$TAG" =~ ^v(.*) ]]; then
              # Meta-package release
              VERSION="${BASH_REMATCH[1]}"
              echo "package=omniscient-architect" >> $GITHUB_OUTPUT
              echo "version=$VERSION" >> $GITHUB_OUTPUT
              echo "package_path=." >> $GITHUB_OUTPUT
              
            elif [[ "$TAG" =~ ^omniscient-([a-z]+)-v(.*) ]]; then
              # Sub-package release
              PACKAGE="${BASH_REMATCH[1]}"
              VERSION="${BASH_REMATCH[2]}"
              echo "package=omniscient-$PACKAGE" >> $GITHUB_OUTPUT
              echo "version=$VERSION" >> $GITHUB_OUTPUT
              echo "package_path=packages/$PACKAGE" >> $GITHUB_OUTPUT
            else
              echo "::error::Invalid tag format: $TAG"
              exit 1
            fi
          fi

      - name: Show parsed values
        run: |
          echo "Package: ${{ steps.parse.outputs.package }}"
          echo "Version: ${{ steps.parse.outputs.version }}"
          echo "Path:    ${{ steps.parse.outputs.package_path }}"
          echo "Dry Run: ${{ steps.parse.outputs.dry_run }}"

  # ============================================================================
  # Build the package
  # ============================================================================
  build:
    name: Build Package
    runs-on: ubuntu-latest
    needs: prepare
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install build tools
        run: |
          python -m pip install --upgrade pip
          pip install build twine

      - name: Validate release metadata
        run: |
          python - <<'PY'
          import pathlib
          import tomllib

          package_path = pathlib.Path("${{ needs.prepare.outputs.package_path }}")
          pyproject_path = package_path / "pyproject.toml"

          if not pyproject_path.exists():
              raise SystemExit(f"Missing package metadata file: {pyproject_path}")

          data = tomllib.loads(pyproject_path.read_text())
          actual_name = data["project"]["name"]
          actual_version = data["project"]["version"]
          expected_name = "${{ needs.prepare.outputs.package }}"
          expected_version = "${{ needs.prepare.outputs.version }}"

          if actual_name != expected_name:
              raise SystemExit(
                  f"Package name mismatch: workflow resolved {expected_name!r}, "
                  f"but {pyproject_path} declares {actual_name!r}"
              )

          if actual_version != expected_version:
              raise SystemExit(
                  f"Version mismatch: workflow resolved {expected_version!r}, "
                  f"but {pyproject_path} declares {actual_version!r}"
              )

          print(f"Validated {actual_name} version {actual_version}")
          PY

      - name: Build package
        run: |
          cd ${{ needs.prepare.outputs.package_path }}
          python -m build

      - name: Check package with twine
        run: |
          cd ${{ needs.prepare.outputs.package_path }}
          twine check dist/*

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dist
          # Uploads the contents of the dist folder to the artifact root
          path: ${{ needs.prepare.outputs.package_path }}/dist/*

  publish-pypi:
    name: Publish to PyPI
    runs-on: ubuntu-latest
    needs: [prepare, build]
    if: needs.prepare.outputs.dry_run == 'false'
    permissions:
      id-token: write
    environment:
      name: pypi
    env:
      PYPI_API_TOKEN: ${{ secrets.PYPI_API_TOKEN }}
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/

      - name: Publish distributions with API token
        if: env.PYPI_API_TOKEN != ''
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist/
          password: ${{ secrets.PYPI_API_TOKEN }}

      - name: Publish distributions with trusted publishing
        if: env.PYPI_API_TOKEN == ''
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist/


  # ============================================================================
  # Create GitHub Release
  # ============================================================================
  github-release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs: [prepare, build, publish-pypi]
    # Only run on tags, not manual triggers, to avoid detaching releases from tags
    if: needs.prepare.outputs.dry_run == 'false' && needs.prepare.outputs.is_manual == 'false'
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          # Explicitly sets tag name to ensure alignment
          tag_name: ${{ github.ref_name }}
          name: ${{ needs.prepare.outputs.package }} v${{ needs.prepare.outputs.version }}
          body: |
            ## ${{ needs.prepare.outputs.package }} v${{ needs.prepare.outputs.version }}
            
            ### Installation
            
            ```bash
            pip install ${{ needs.prepare.outputs.package }}==${{ needs.prepare.outputs.version }}
            ```
            
            ### PyPI Links
            
            - [PyPI](https://pypi.org/project/${{ needs.prepare.outputs.package }}/${{ needs.prepare.outputs.version }}/)
            
            ### Changes
            
            See [CHANGELOG.md](https://github.com/${{ github.repository }}/blob/main/CHANGELOG.md) for release details.
          files: dist/*
          draft: false
          # Checks for rc, beta, alpha anywhere in the version string
          prerelease: ${{ contains(needs.prepare.outputs.version, 'rc') || contains(needs.prepare.outputs.version, 'beta') || contains(needs.prepare.outputs.version, 'alpha') }}
